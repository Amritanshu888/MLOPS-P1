- Add the files : .gitignore and README.md
- Do : git init
- After adding commit it : git add . then git commit -m "gitignore is commited"
- Then git push origin main

## Some libraries in python which can make project structure : cookie-cutter
- In dagshub connect with the github repo of this project. From there choose the repository u want to connect to. Connecting ur github repository with dagshub. With dagshub u will be able to log experiments , u will be able to make data versioning and model versioning.

- Configure everything : all configure codes in Remote -> Data -> DVC and Remote -> Experiments.

- The pipeline which we have created here in the main.py is the Training Pipeline.

- Now we will create our Prediction Pipeline.